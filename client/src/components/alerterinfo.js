import './usm.css';
const AlerterInfo = () => {
    return ( 
    <div id="alerterinfo-container">
        <div>Name: Jane Doe </div>
        {/* when queries can be made succesfully, (heartrate is done and name is less important), this will be implemented properly. */}
        <div>Heart Rate: </div>
    </div>
     );
}
 
export default AlerterInfo;
